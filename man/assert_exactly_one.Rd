% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arguments.R
\name{assert_exactly_one}
\alias{assert_exactly_one}
\title{Assert that exactly one of several arguments is supplied}
\usage{
assert_exactly_one(..., call = rlang::caller_env())
}
\arguments{
\item{...}{Two or more arguments to check, typically optional parameters of
the calling function that default to \code{NULL}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Treats a \code{NULL} argument as "not supplied". Raises an error unless exactly
one argument in \code{...} is non-\code{NULL}.
}
\examples{
connect <- function(url = NULL, socket = NULL) {
  assert_exactly_one(url, socket)
  "ok"
}
connect(url = "localhost")

}
