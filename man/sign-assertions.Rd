% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values.R
\name{sign-assertions}
\alias{sign-assertions}
\alias{assert_all_positive}
\alias{assert_all_non_negative}
\alias{assert_all_negative}
\alias{assert_all_non_positive}
\title{Assert the sign of all values}
\usage{
assert_all_positive(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_non_negative(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_negative(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_non_positive(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Each function checks that \code{x} is numeric and that every value satisfies the
sign constraint. Missing values cause the check to fail.
}
\examples{
assert_all_positive(c(1, 2, 3))
assert_all_non_negative(c(0, 1, 2))

}
