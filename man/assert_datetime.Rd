% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{assert_datetime}
\alias{assert_datetime}
\title{Assert that an object is a date-time}
\usage{
assert_datetime(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \code{x} is a date-time, that is, of class \code{POSIXct} (as returned by
\code{\link[=Sys.time]{Sys.time()}}, \code{\link[=as.POSIXct]{as.POSIXct()}}, and lubridate functions such as \code{ymd_hms()}).
Allows any length; for a single value use \code{\link[=assert_scalar_datetime]{assert_scalar_datetime()}}.
}
\examples{
assert_datetime(Sys.time())

}
