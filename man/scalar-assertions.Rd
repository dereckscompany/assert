% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{scalar-assertions}
\alias{scalar-assertions}
\alias{assert_scalar_character}
\alias{assert_scalar_numeric}
\alias{assert_scalar_integer}
\alias{assert_scalar_double}
\alias{assert_scalar_logical}
\alias{assert_scalar_complex}
\title{Assert that an object is a single value of a given type}
\usage{
assert_scalar_character(
  x,
  null_ok = FALSE,
  non_empty = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_scalar_numeric(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_scalar_integer(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_scalar_double(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_scalar_logical(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_scalar_complex(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{non_empty}{Single logical, used by \code{\link[=assert_scalar_character]{assert_scalar_character()}} only. If
\code{TRUE}, the empty string \code{""} is rejected as well. Defaults to \code{FALSE}.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Each function checks that \code{x} has length exactly 1, is of the named type,
and is not \code{NA}. (A single value that is secretly \code{NA} is almost always a
bug, so it is rejected here; use the plain vector checks such as
\code{\link[=assert_character]{assert_character()}} if you want to allow \code{NA}.) On success the input is
returned invisibly so checks can be stacked; on failure an error is raised
against the calling function.
}
\details{
\code{\link[=assert_scalar_character]{assert_scalar_character()}} additionally accepts \code{non_empty}: set it to
\code{TRUE} to reject the empty string \code{""} as well, for arguments such as
identifiers or codes where an empty value is never valid.
}
\examples{
assert_scalar_character("hello")
assert_scalar_numeric(42)
assert_scalar_logical(TRUE)

# Optional argument that defaults to NULL:
assert_scalar_numeric(NULL, null_ok = TRUE)

# Reject empty strings for identifiers and codes:
assert_scalar_character("BTC/USDT", non_empty = TRUE)

}
