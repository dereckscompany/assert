% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_frame.R
\name{assert_columns_are_type}
\alias{assert_columns_are_type}
\title{Assert that a set of columns share a single type}
\usage{
assert_columns_are_type(
  x,
  columns,
  type,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{columns}{Character vector of column names to check.}

\item{type}{Single string naming the type that every listed column must be.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \code{x} is a data frame containing every name in \code{columns}, and
that all of those columns are of \code{type}. Type names are matched as described
in \code{\link[=assert_column_types]{assert_column_types()}}.
}
\examples{
measurements <- data.frame(height = 1.8, weight = 75.0)
assert_columns_are_type(measurements, c("height", "weight"), "numeric")

}
