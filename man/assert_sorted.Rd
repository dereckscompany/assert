% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relational.R
\name{assert_sorted}
\alias{assert_sorted}
\title{Assert that values are sorted}
\usage{
assert_sorted(
  x,
  decreasing = FALSE,
  strictly = FALSE,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{decreasing}{Single logical: if \code{TRUE}, require descending order
instead of ascending. Defaults to \code{FALSE}.}

\item{strictly}{Single logical: if \code{TRUE}, require a strict order with no
tied (equal) neighbouring values. Defaults to \code{FALSE}.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \code{x} is in sorted order. By default the order must be
non-decreasing (ties allowed). Missing values cause the check to fail.
}
\examples{
assert_sorted(c(1, 2, 2, 3))
assert_sorted(c(3, 2, 1), decreasing = TRUE)
assert_sorted(c(1, 2, 3), strictly = TRUE)

}
