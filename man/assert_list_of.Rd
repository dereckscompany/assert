% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basics.R
\name{assert_list_of}
\alias{assert_list_of}
\title{Assert that every element of a list is of a given type}
\usage{
assert_list_of(
  x,
  type,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{type}{Single string naming the type every element must be.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \code{x} is a list and that each of its elements is of \code{type}. Type
names are matched as in \code{\link[=is.character]{is.character()}}, \code{\link[=is.numeric]{is.numeric()}}, \code{\link[=is.integer]{is.integer()}},
\code{\link[=is.double]{is.double()}}, \code{\link[=is.logical]{is.logical()}}, \code{\link[=is.complex]{is.complex()}}, \code{\link[=is.factor]{is.factor()}}, and
\code{\link[=is.list]{is.list()}}; any other name is matched against each element's class with
\code{\link[=inherits]{inherits()}} (for example "Date").
}
\examples{
assert_list_of(list("a", "b", "c"), "character")
assert_list_of(list(1, 2, 3), "numeric")

}
