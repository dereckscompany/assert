% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arguments.R
\name{assert_at_least_one}
\alias{assert_at_least_one}
\title{Assert that at least one of several arguments is supplied}
\usage{
assert_at_least_one(..., call = rlang::caller_env())
}
\arguments{
\item{...}{Two or more arguments to check, typically optional parameters of
the calling function that default to \code{NULL}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Treats a \code{NULL} argument as "not supplied". Raises an error if every
argument in \code{...} is \code{NULL}.
}
\examples{
fetch <- function(id = NULL, name = NULL) {
  assert_at_least_one(id, name)
  "ok"
}
fetch(name = "ada")

}
