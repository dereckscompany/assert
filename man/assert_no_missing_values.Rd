% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values.R
\name{assert_no_missing_values}
\alias{assert_no_missing_values}
\title{Assert that an object contains no missing values}
\usage{
assert_no_missing_values(
  x,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \code{x} contains no missing values, that is, no \code{NA}. Note that R
also reports \code{NaN} ("Not a Number", such as \code{0 / 0}) as missing via
\code{\link[=is.na]{is.na()}}, so \code{NaN} is caught here too. To reject \code{NaN} and infinite values
specifically as invalid numbers, use \code{\link[=assert_all_finite]{assert_all_finite()}}.
}
\examples{
assert_no_missing_values(c(1, 2, 3))

}
