% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/length.R
\name{assert_minimum_length}
\alias{assert_minimum_length}
\alias{assert_maximum_length}
\title{Assert that an object's length falls within bounds}
\usage{
assert_minimum_length(
  x,
  minimum_length,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_maximum_length(
  x,
  maximum_length,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{minimum_length}{Single non-negative whole number: the smallest
allowed length.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}

\item{maximum_length}{Single non-negative whole number: the largest
allowed length.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Assert that an object's length falls within bounds
}
\examples{
assert_minimum_length(1:5, 3)

assert_maximum_length(1:5, 10)

}
