% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relational.R
\name{comparison-assertions}
\alias{comparison-assertions}
\alias{assert_all_greater_than}
\alias{assert_all_greater_than_or_equal}
\alias{assert_all_less_than}
\alias{assert_all_less_than_or_equal}
\title{Assert element-wise numeric comparisons}
\usage{
assert_all_greater_than(
  x,
  threshold,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_greater_than_or_equal(
  x,
  threshold,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_less_than(
  x,
  threshold,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

assert_all_less_than_or_equal(
  x,
  threshold,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{threshold}{A single number, or a numeric vector the same length as
\code{x}, to compare against.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Each function checks that \code{x} is numeric and that every element compares as
required against \code{threshold}. \code{threshold} may be a single number (compared
against every element of \code{x}) or a numeric vector the same length as \code{x}
(compared element by element). Missing values cause the check to fail.
}
\examples{
assert_all_greater_than(c(2, 3, 4), 1)
assert_all_less_than_or_equal(c(1, 2), c(1, 5))

}
