% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/length.R
\name{assert_length_between}
\alias{assert_length_between}
\title{Assert that an object's length falls within an inclusive range}
\usage{
assert_length_between(
  x,
  minimum_length,
  maximum_length,
  null_ok = FALSE,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{Object to check.}

\item{minimum_length}{Single non-negative whole number: the smallest allowed
length.}

\item{maximum_length}{Single non-negative whole number: the largest allowed
length.}

\item{null_ok}{Single logical. If \code{TRUE}, a \code{NULL} value passes the check
without error. Use this for optional arguments that default to \code{NULL}.
Defaults to \code{FALSE}, so \code{NULL} is rejected unless you opt in.}

\item{arg}{Name used to refer to \code{x} in error messages. Defaults to the
name of the expression passed as \code{x}.}

\item{call}{Environment used as the error's call context. Defaults to the
calling function, so errors point at the user's code.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Checks that \verb{minimum_length <= length(x) <= maximum_length}, both inclusive.
The single-call form of stacking \code{\link[=assert_minimum_length]{assert_minimum_length()}} and
\code{\link[=assert_maximum_length]{assert_maximum_length()}}.
}
\examples{
assert_length_between(1:5, 1, 10)

}
